/**
 * 시간 상수(제품·테스트 공용 단일 출처).
 *
 * 제품과 E2E 가 같은 값을 파생하도록 여기서만 정의한다 — 매직넘버가 두 곳에 흩어지면
 * 한쪽만 바뀌었을 때 조용히 레이스가 생긴다(§heavy-queue-wait 사건).
 */

/**
 * 승인 대기 큐의 저빈도 폴백 폴링 주기(ms).
 *   진실 소스는 `QUEUE_UPDATED` 신호(즉시 재조회)이고, 신호를 놓친 구간은 이 폴링이 메운다.
 *   → 폴이 정시에 돌고 refresh 가 성공하면 **이 주기 안에 큐행이 뜨는 것**이 설계 보증이다.
 *
 *   E2E 대기 불변식: 큐행 가시성을 기다리는 대기는 이 값의 **2배를 초과**해야 한다.
 *   ("1주기 초과"가 아니라 "1주기 유실을 견딤"이 옳은 하한 — 부하로 폴 N+1 의 refresh 가 유실되면
 *    보증까지 최대 2주기가 걸린다.) 파생·머신체크는 `e2e/helpers.ts:QUEUE_ROW_WAIT_MS`
 *    (= QUEUE_POLL_MS × 2.5, 로드 시점에 하한 2× 위반이면 실패).
 */
export const QUEUE_POLL_MS = 20_000;
